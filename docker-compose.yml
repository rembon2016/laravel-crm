version: "3.9"

services:
  app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: krayin-crm:latest
    container_name: krayin-app-container
    restart: unless-stopped
    ports:
      - "8600:80"
    volumes:
      - .:/app

volumes:
  pgdata:
